rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // MVP: open access. Replace with proper auth rules before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
